- @toys.each do |toy|
  %li.item
    .item__details
      = link_to toy do
        %figure
          .item-image
            %span 
              ¥
              = toy.price 

            - if toy.images.attached?
              - toy.images.each do |image|
                = image_tag image

            - if toy.buyer_id.present? 
              .items-box_photo__sold
                .items-box_photo__sold__inner
                  SOLD

          %figcaption
            %span 
              = toy.name